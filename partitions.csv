# Name,   Type, SubType, Offset,   Size,     Flags
# All card assets (images + audio) live on the microSD card, so this build
# needs no on-flash data partition -- the whole 16MB is available to the app.
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  0xF00000,
